# ============================================================
# 换行符规范化
#
# 背景：Windows 上 core.autocrlf=true 会让 Git 在检出时把 LF 换成 CRLF。
# 对 .sh 脚本这是致命的——CRLF 的 shell 脚本在 Linux 上执行会报
#     bash: $'\r': command not found
# 而 deploy/deploy.sh 恰恰是给阿里云 Ubuntu 服务器用的，
# 一旦被检出成 CRLF，服务器上会满屏这个报错，且很难联想到换行符。
#
# 这里显式钉死：shell 脚本永远以 LF 入库、以 LF 检出，与本机 Git 配置无关。
# ============================================================

*.sh text eol=lf
